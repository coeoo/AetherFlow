# CVE 检索工作台使用说明

> **如何使用 CVE 补丁检索**

---

## 1. 功能简介

该工作台用于输入一个 `CVE ID`，让系统主动检索补丁、展示运行过程，并提供可复查的补丁证据。

---

## 2. 使用步骤

1. 打开页面 `/cve`
2. 在输入框中输入 CVE，例如 `CVE-2024-3094`
3. 点击“开始检索”
4. 等待运行状态、最近进展和结果摘要更新
5. 点击“查看详情”进入证据页继续复核
6. 页面底部的“最近运行”会在当前 run 进入终态后自动同步刷新，无需手动重提
7. 也可以直接从“最近运行”区域回看刚刚跑过的记录

---

## 3. 页面说明

- **输入框**：仅接受合法 CVE 编号
- **运行状态卡片**：展示当前阶段与最近进展
- **结果摘要**：展示是否找到补丁、主证据与下一步操作
- **最近运行**：展示最近几次 run 的主证据或失败原因，并在当前 run 终态后自动同步
- **详情页入口**：进入完整 trace、patch、diff 查看页
- **失败态进度**：如果中途失败，会保留真实失败阶段，而不是显示“全部完成”

---

## 4. 常见问题

### 为什么一直显示运行中？
- 说明后台还在探索引用页面或下载 patch。
- 若长时间无进展，请查看详情页中的停止原因或联系维护者。

### 为什么没有找到补丁？
- 并非所有 CVE 都能直接命中可下载 patch。
- 详情页会展示系统探索过的页面、停止原因，以及建议先排查的失败步骤。
- 当前系统会先查询 CVE 官方记录，再补充 OSV、GitHub Advisory 和 NVD。
- 如果 seed 里已经直接出现 `.patch`、`.diff`、GitHub / GitLab commit 等高信号入口，系统会优先消费这些候选，再补抓普通页面。
- 单个页面失败不一定会让整次运行失败；只有在没有其他可继续消费的页面或候选时，才会收口为 `抓取页面失败`。

### 为什么 patch 不能查看 diff？
- 可能该候选未下载成功，或原始内容文件已不存在。
- 当前详情页内部会优先按稳定的 `patch_id` 读取 diff 内容。

### 为什么详情页里会看到“共 2 条记录”？
- 说明同一个 patch URL 在运行过程中被重复发现或重复落表。
- 页面只展示一条代表记录，但会保留重复数量，方便排障和后续补偿。

### 为什么 patch 类型不再只显示 Patch / Diff？
- 当前规则库已经支持更多补丁入口，例如 `Debdiff`、`GitHub PR Patch`、`GitLab Commit Patch`、`GitLab MR Patch`、`Kernel Commit Patch`、`Bugzilla Attachment Patch`。
- 这些标签只是展示文案，底层仍然会保留真实下载地址和证据 trace。

### 为什么“最近运行”之前会显示 queued，现在会自动更新？
- 当前工作台会先把新 run 写入最近运行列表，然后轮询 active run 详情。
- 当 active run 进入 `succeeded` 或 `failed` 终态时，页面会自动刷新最近运行列表一次，保证顶部摘要和底部历史状态一致。

---

## 5. 结果解释

- **已命中补丁**：系统找到了可用 patch/diff，并保留了内容或下载结果。
- **Patch 可查看**：按钮可点，说明 Artifact 与磁盘文件都仍然存在。
- **共 N 条记录**：同一 `candidate_url` 有多条落表记录，页面已合并展示。
- **Debdiff / GitHub PR Patch / GitLab / Kernel / Bugzilla Attachment Patch**：表示系统通过非 NVD 规则或单页提取识别到了不同类型的补丁入口。
- **未命中**：系统完成探索，但没有找到满足规则的 patch。

---

## 🔄 变更记录

### v1.0 - 2026-04-09
- 初始化 CVE 工作台使用说明

### v1.1 - 2026-04-13
- 回填工作台与详情页最小闭环的实际操作说明
- 补充失败态进度、diff 不可用和重复 patch 记录的解释

### v1.2 - 2026-04-15
- 增加最近运行列表的使用说明
- 把 stop reason 文案更新为更可读的“停止原因”表达
- 补充详情页按 `patch_id` 读取 diff 的说明

### v1.3 - 2026-04-15
- 补充失败详情页的排障表达：错误摘要、建议动作和最近失败步骤

### v1.4 - 2026-04-15
- 同步官方优先多源 seed 已进入当前主链
- 补充新增 patch 类型标签的用户说明
- 增补“为什么 patch 类型不再只显示 Patch / Diff”的解释

### v1.5 - 2026-04-16
- 同步主链已支持 seed 直达 patch/commit 优先消费。
- 同步页面抓取改为局部容错，不再默认被单页失败拖死整次运行。
- 补充最近运行列表在 active run 终态后自动同步刷新的说明。

---

**文档版本**：v1.5
**创建日期**：2026-04-09  
**最后更新**：2026-04-16
