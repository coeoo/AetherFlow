# 核心特性

> **AetherFlow v1 目标能力矩阵**

> 读前建议：先阅读 [`总体设计`](../00-总设计/总体项目设计.md)。本文描述 v1 的 **目标能力**，不代表当前已落地的工程事实。当前仓库仍处于文档与设计阶段。

---

## 🎯 版本目标

v1 的目标是建成一个有真实场景路径的情报处理平台，而非抽象框架：

- **CVE 补丁检索** — 输入 CVE 编号，主动检索并下载补丁
- **安全公告提取** — 从安全公告中提取结构化情报

承载平台要把这两条路径都承载起来，同时沉淀真正可复用的共用底座。

---

## ✨ 核心特性总览

### 平台共用能力
- **统一任务底座**：手动触发、定时触发、失败重试、后台执行。
- **统一文档采集基座**：URL 抓取、正文归一化、原文快照、Artifact 持久化。
- **统一投递能力**：邮件、微信/企业微信、Webhook 三类真实渠道。
- **统一工作台入口**：平台首页统一展示场景入口、最近任务、最近投递和系统状态。
- **统一证据保留**：分析结果、证据片段、原文和附件都能复查。

### CVE 场景能力
- **多源聚合**：同时查询多个权威来源。
- **页面探索**：沿可信链接继续查找补丁候选。
- **补丁下载**：下载 patch/diff 并保留原始内容。
- **结果解释**：展示 stop reason、trace、fix family 和 patch 证据。
- **差异查看**：直接查看补丁 diff 内容。

### 安全公告场景能力
- **手动提取**：支持 URL 与正文粘贴两种入口。
- **监控提取**：支持配置监控源、调度频率、通知目标和手动试跑。
- **结构化情报包**：输出受影响对象、IOC、风险判断、修复建议和证据片段。
- **多源适配**：首批支持微信文章源、Openwall、NCC Security Community。
- **结果投递**：按策略推送到邮件、微信/企业微信、Webhook。

---

## 📊 目标能力矩阵

> 以下为 v1 的设计目标，不是已实现状态。

| 能力 | 平台 | CVE | 安全公告 |
|------|------|-----|----------|
| 平台首页与场景入口 | ● | - | - |
| 后台任务执行 | ● | 复用 | 复用 |
| 定时调度 | ● | 预留 | ● |
| 文档采集与 Artifact | ● | 复用 | 复用 |
| 邮件/微信/Webhook 投递 | ● | 可接入 | ● |
| 多源查询/抓取 | - | ● | ● |
| 页面探索/证据追踪 | 基座支持 | ● | 局部复用 |
| 结构化情报结果 | 基座支持 | 补丁结果 | 公告情报包 |
| 运行详情页 | 基座支持 | ● | ● |

- ● = v1 目标能力

---

## 🚫 当前不包含的能力

- 多租户与复杂权限体系
- 旧 `pipeline / product / diagnostics` 语义兼容
- CVE legacy lookup API
- 开放式网页代理或通用多 agent 平台
- 完整的 SaaS 运维面板

---

## 🧭 产品阅读顺序

建议按下面顺序理解产品：

1. [`产品概述`](产品概述.md)
2. [`架构设计`](../03-系统架构/架构设计.md)
3. [`功能设计`](../04-功能设计/README.md)
4. 各功能模块详细设计文档

---

## 🔄 变更记录

### v1.0 - 2026-04-09
- 初始化平台能力矩阵
- 明确平台共用能力与场景专属能力边界

### v1.1 - 2026-04-10
- 修正为目标能力矩阵，与当前项目阶段对齐
- 加入阶段说明，明确当前仓库仍处于文档与设计阶段

---

**文档版本**：v1.1
**创建日期**：2026-04-09
**最后更新**：2026-04-10
**维护人**：AI + 开发团队
