# U002 视觉基线与继承策略

> **基于备份仓 UI 资产的视觉方向收口文档**

> 本文只固定“当前页面规格应遵循的视觉方向”，不输出高保真视觉稿或完整设计 token。

---

## 🎯 目标

当前新仓的页面设计需要继承备份仓已经验证过的 UI 优点，但必须显式剔除旧叙事与旧后台感。

这轮的视觉策略固定为：**继承增强**。

含义：

- 继承备份仓的轻玻璃、浅色层级、Manrope 排版和结论优先卡片结构。
- 保留 CVE 场景中已经证明有效的“先讲结论，再给证据”的页面组织。
- 按新仓“平台首页 + 场景工作台”的边界重写页面，不把旧实现直接伪装成现成页面。

---

## 🪞 已继承的视觉原则

来自备份仓 `docs/13-界面设计/README.md` 和 `STYLE_GUIDE.md` 的方向如下：

1. **Tonal Surfaces**：用浅色层级区分卡片与区域，不依赖重边框。
2. **Glass Header**：顶部导航可以使用轻玻璃风格，但不压过内容。
3. **Hyper Typography**：标题、摘要、标签之间要有明确字号和字重差。
4. **Semantic States**：鲜明颜色只用于状态与关键结论，不用于大面积铺底。
5. **Conclusion First**：主结论卡优先于原始 trace、列表和 JSON。

---

## 🎨 当前 v1 的视觉基线

### 1. 整体气质

- 浅色底，不走深色安全看板路线。
- 精准、克制、偏分析台，而不是炫技式监控大屏。
- 页面默认以卡片和区块分层，不以大表格为主界面。

### 2. 排版基线

- 主字体继续使用 `Manrope` 方向。
- 页面首屏必须有一句话结论或定位，不允许一进页就是参数表。
- 状态标签使用全大写或短标签，避免长句挤占视觉焦点。

### 3. 颜色基线

- 主背景：浅灰白系。
- 主文案：高对比黑/深灰。
- 状态色只用于关键节点：
  - 绿色：成功、可投递、健康
  - 红色：失败、阻断、异常
  - 蓝灰：运行中、待处理、信息态
  - 黄色：重复提示、部分失败、人工关注

### 4. 组件基线

- 首选卡片化布局，而不是大段表格。
- 输入控件采用厚实浅灰底 + 聚焦强化边界的方式。
- Diff、大文本、证据片段使用独立阅读区，不与摘要混在同一卡片。

---

## 🏗️ 视觉层级总图

```mermaid
flowchart TD
    subgraph 页面层级
        direction TB
        GL["Glass Header\n轻玻璃顶导航"]
        HC["Hero / Conclusion Card\n主结论首屏"]
        SS["Structured Sections\n结构化信息区"]
        EV["Evidence / Detail\n证据与次级内容"]
        ACT["Actions / Delivery\n操作与投递区"]
    end

    GL --> HC
    HC --> SS
    SS --> EV
    EV --> ACT

    subgraph 颜色语义
        direction LR
        G["🟢 成功/健康/可投递"]
        R["🔴 失败/阻断/异常"]
        B["🔵 运行中/待处理/信息"]
        Y["🟡 重复/部分失败/关注"]
    end

    style 页面层级 fill:#f8f8f8,stroke:#b0b0b0,color:#333
    style 颜色语义 fill:#fafafa,stroke:#c0c0c0,color:#333
```

---

## 🧱 各页面的视觉要求

### 平台首页

- 首屏必须同时出现平台定位和两个场景入口。
- 场景入口是主视觉焦点，最近任务与健康是次级区块。
- 不把首页做成运维面板。

### CVE 工作台与详情

- 继承备份仓 `CVELookupPage` 与 `CVELookupCard` 的组织逻辑。
- 工作台页面首屏聚焦输入、当前运行和结论摘要。
- 详情页首屏聚焦主补丁结论、可信原因和下一步建议。
- Trace 和 diff 属于次级但可展开内容。

### 公告工作台与详情

- 手动提取页首屏聚焦输入模式切换和当前结果预览。
- 监控页面聚焦批次摘要，不把单文档详情混进批次主表。
- 情报包详情页首屏聚焦分析师摘要、风险级别和受影响对象。

### 平台工具页

- 投递中心和系统页允许更高信息密度，但仍保持卡片层级和浅色基底。
- 工具页使用次级导航语义，不把自己做成系统后台首页。
- 任务中心、投递记录和系统状态都要强调“可行动的信息”，而不是裸数据墙。

---

## 🚫 明确不继承的方向

- 旧后台壳的多级系统菜单
- 以工程调试字段为主的首页
- 一屏塞满表格、筛选器和管理控件的后台审计式界面
- 紫色、荧光色、重阴影、重金属暗色风格

---

## 🔁 参考资产到新仓页面的映射

| 参考资产 | 新仓继承点 | 新仓改写点 |
|----------|------------|------------|
| `REFERENCE_PROTOTYPE.html` | 浅色层级、玻璃导航、Manrope 字体方向 | 去掉原型级 Tailwind 表述，改为页面规格 |
| `CVELookupPage.tsx` | 输入区与工作流首屏节奏 | 改成文档化的区块与状态稿 |
| `CVELookupCard.tsx` | 主结论卡、可信原因、下一步建议 | 删除与旧数据结构深耦合的实现细节 |
| `PatchDiffViewer.tsx` | Diff 独立阅读区 | 作为 `P102` 的大文本查看约束 |

---

## ✅ 设计落地标准

- 任一页面首屏都能在 3 秒内回答“这里能做什么”和“当前结果是什么”。
- 主结论卡片始终早于工程细节区块。
- 页面视觉分层主要靠背景层级和间距，而不是依赖 1px 边框堆叠。
- 同一场景内的页面在标题、状态胶囊和操作区布局上保持一致语言。

---

## 🔄 变更记录

### v1.0 - 2026-04-09
- 新增视觉基线与备份仓继承策略
- 固定当前 v1 的页面气质、排版和状态表达方向
- 明确哪些旧视觉语义不再进入新仓主线

### v1.1 - 2026-04-10
- 增加平台工具页的视觉约束
- 明确工具页允许更高信息密度，但不回流到旧后台壳风格

---

**文档版本**：v1.1  
**创建日期**：2026-04-09  
**最后更新**：2026-04-10  
**维护人**：AI + 开发团队
