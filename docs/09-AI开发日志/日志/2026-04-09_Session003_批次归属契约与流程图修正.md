# 2026-04-09 Session003 - 批次归属契约与流程图修正

状态: ✅ 已完成

## 做了什么

- 基于追加评审，修正了监控批次与监控源之间的强约束表达。
- 为 `source_fetch_records` 增加了可空 `source_id` 契约，用于公告监控批次与 `announcement_sources` 的稳定关联。
- 修正了 M002 流程图中的旧语义，去掉“创建新 attempt/任务”的歧义表达。

## 本次更新的文档

- `docs/03-系统架构/数据库设计.md` — 为 `source_fetch_records` 增加 `source_id` 字段说明和批次归属契约
- `docs/04-功能设计/M203-安全公告调度运行与结果功能设计.md` — 明确监控批次必须带 `source_id`
- `docs/04-功能设计/M002-平台任务执行与调度中心功能设计.md` — 修正重试流程图
- `docs/05-数据库/sql/2026-04-09_init_platform_core.sql` — 为 `source_fetch_records` 增加 `source_id` 外键
- `docs/05-数据库/sql/2026-04-09_init_indexes.sql` — 增加 `source_fetch_records(source_id, created_at)` 索引

## 本次更新的代码

- 无代码改动，仍为设计与 SQL 草案修正

## 关键决策

- 监控批次到监控源的归属不再只靠 `source_ref` 弱语义，而是通过 `source_id` 落成可空外键。
- `source_fetch_records` 仍保留平台表身份，因此 `source_id` 只在公告监控批次场景中要求非空。
- 平台重试流程图必须与正文契约一致，统一为“只新增 attempt，不新增任务或 run”。

## 问题与处理

- 问题：`source_fetch_records` 与 `announcement_sources` 的关系只在文档图里存在，没有字段级硬约束。
- 处理：增加 `source_id` 并同步到数据库设计、功能设计和 SQL 草案。

## 下次计划

1. 如继续进入实现，优先先把公告监控数据模型做成真实迁移
2. 再实现平台任务层和公告批次查询接口

## 遗留风险

- 目前 `source_fetch_records.source_id` 的“公告监控批次时必须非空”仍是文档契约，正式实现时应通过服务层校验或数据库检查约束强化

