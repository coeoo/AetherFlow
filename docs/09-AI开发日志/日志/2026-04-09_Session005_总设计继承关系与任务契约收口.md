# 2026-04-09 Session005 - 总设计继承关系与任务契约收口

状态: ✅ 已完成

## 做了什么

- 根据新增评审反馈，继续收口了总设计母文档与下位文档之间的继承关系。
- 修正了产品入口文档中仍把系统首定义写成“双场景平台”的口径冲突。
- 在架构设计中重新显式保留了“情报处理引擎内核 + 当前 v1 平台壳”的双层边界。
- 收紧了 `task_job` 契约，把 v1 的 `scene_name` 固定为 `cve/announcement`，避免与“一个 job 绑定一个场景 run”的语义冲突。
- 补齐了 `M901` 的母文档阅读入口，并修正了总设计文档中的备份仓相对路径。

## 本次更新的文档

- `docs/01-产品介绍/快速开始.md` — 修正系统首定义
- `docs/01-产品介绍/产品概述.md` — 修正项目类型与产品定位
- `docs/03-系统架构/架构设计.md` — 补双层架构边界和监控批次流程表达
- `docs/03-系统架构/数据库设计.md` — 收紧 `task_jobs` 契约
- `docs/04-功能设计/M002-平台任务执行与调度中心功能设计.md` — 统一 task_job 只承载场景 run
- `docs/04-功能设计/M203-安全公告调度运行与结果功能设计.md` — 明确抓取批次与场景任务分层
- `docs/04-功能设计/M901-功能模块关系与开发顺序设计.md` — 补母文档入口和职责降级说明
- `docs/00-总设计/总体项目设计.md` — 修正备份仓相对路径
- `docs/05-数据库/sql/2026-04-09_init_platform_core.sql` — 为 `task_jobs.scene_name` 增加 v1 约束

## 本次更新的代码

- 无代码改动，仍为文档与 SQL 草案收口

## 关键决策

- 新仓的首定义继续保持为“情报处理引擎”，当前平台壳只是承载形态。
- `task_jobs` 在 v1 中只承载场景执行任务，不承担无场景 run 的纯平台批次记录。
- 公告监控批次继续落在 `source_fetch_records`，单文档提取才创建 `announcement_run + task_job`。

## 问题与处理

- 问题：产品入口文档与总设计母文档的首定义冲突。
- 处理：把入口文档改回“引擎内核定义 + 当前 v1 平台壳承载”的表述。

- 问题：下位架构文档把双层架构压平成工程分层，削弱了母文档边界。
- 处理：在架构设计中显式增加“双层边界”说明，并把场景服务层收口为“情报处理引擎内核层”。

- 问题：`task_job` 的字段允许值和运行时契约不一致。
- 处理：把 v1 的 `scene_name` 固定为 `cve/announcement`，并把不带场景 run 的平台批次记录排除出 `task_jobs`。

## 下次计划

1. 如果继续做文档评审，可进一步检查 `核心特性.md` 是否也需要显式继承母文档
2. 如进入实现阶段，应先把母文档中的双层架构投影到目录结构和运行时接口

## 遗留风险

- 当前这轮已经把总纲继承关系和 `task_job` 契约收口，但真正进入实现时，仍要继续警惕把“平台壳工程分层”误当成“系统核心定义”
